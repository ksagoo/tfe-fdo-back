if fqdn in hostnames and access_group.lower() in config_name.lower():
    result["found"] = True
    result["matches"].append({
        "id": config_id,
        "name": config_name,
        "matchType": "full",
        "reason": "fqdn_and_access_group"
    })
    if debug:
        print(f"[DEBUG] FULL MATCH: {config_name} (ID: {config_id}) — stopping further checks.")
    break  # Stop iterating through remaining configs
