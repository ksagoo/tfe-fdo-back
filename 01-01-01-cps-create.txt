encoded_group = urllib.parse.quote(group_name)
    encoded_server_url = urllib.parse.quote("ldaps://faa-lds-prod.uk.hsbc")

    url = (
        f"{base_url}?"
        f"serverUrl={encoded_server_url}&"
        f"baseDn=OU%3DCyber%20WASP%20Automation,OU%3DApplications,OU%3DProd,DC%3Dhsbc,DC=com&"
        f"InfoDir={encoded_group}&limit=100&ad=false&sentFrom=CyberWASP"
    )
