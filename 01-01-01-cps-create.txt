# Determine effective threshold (days-to-expiry overrides expiry-threshold)
threshold = args.days_to_expiry or args.expiry_threshold

discovered = discover_enrollments(
    session,
    base_url,
    args.section,
    expiry_threshold=threshold,
    dry_run=args.dry_run,
    debug=args.debug
)
