if debug:
    import json
    with open(os.path.join(OUTPUT_DIR, "raw_enrollments.json"), "w", encoding="utf-8") as f:
        json.dump(response.json(), f, indent=2)
    print("[DEBUG] Saved raw enrollment JSON to output/raw_enrollments.json")
